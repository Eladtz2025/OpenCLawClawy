# Article Images Plan

## Goal
Move from generic gradient covers to real article images when feasible.

## Preferred order
1. Reuse public article hero image if directly available and stable
2. Fallback to source-branded visual block if no image is reliably extractable
3. Last fallback: gradient cover

## Runtime note
Image extraction is source-dependent and may remain partial in blocked runtimes.
