{
  "name": "pc-guardian",
  "version": "1.2.0",
  "private": true,
  "description": "Reporting-only PC and OpenClaw health monitor",
  "scripts": {
    "validate": "node scripts/validate-config.js",
    "check": "node scripts/run-checks.js",
    "dashboard": "node scripts/render-dashboard.js",
    "test": "node scripts/validate-config.js && node scripts/run-checks.js --dry-run"
  }
}
