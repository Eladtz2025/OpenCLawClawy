- ב-news-dashboard, אחרי rerun אמיתי, תוקן mismatch של timestamp בין live-site HTML לבין daily-summary/state.
- commit חדש: 1171f3b (`news-dashboard: refresh run and tighten cleanup`).
- הלקח שנאכף שוב: לא לומר "מושלם" כשעדיין נשארים PT News summary גולמי ותקצירים בינוניים/חתוכים; אלעד מעדיף כנות מפורשת על פני סגירה אופטימית.
- המצב המאומת האחרון לאחר rerun: render SUCCESS, pipeline status PARTIAL, 21 items, technology 4/5, technology2 5/5, israel 5/5, crypto 5/5, hapoel 2/5.
- Worked extensively on `news-dashboard` public GitHub Pages reliability and content quality.
- Fixed Pages publishing path issues earlier, added visible build marker/timestamp in dashboard header, and tied `latest.html` cache-busting to `buildId`.
- Added `news-dashboard/build-sanity-check.js` to validate build consistency (buildId in state/summary/html, local media present, no corruption markers) before considering a build healthy.
- Adjusted topic logic so `hapoel` is treated as low-volume by design and does not make the whole dashboard `PARTIAL` when few legitimate items exist.
- Changed `technology2` from hard cap 5 to effectively unbounded within the last 24h after dedupe/editor filtering, so it can show all distinct good items instead of truncating to 5.
- Important recurring issue discovered: public HTML sometimes showed mojibake/corruption even when local JSON/HTML looked correct. Root cause was around published artifact encoding/dirty tracked HTML state; resolved by forcing clean rewrite of tracked published HTML files and pushing commit `638ff6b` (`Rewrite published dashboard HTML in clean UTF-8`).
- Key commits during this phase included `53f92cf` (hardening/sanity), `9dcad06` (hapoel low-volume + tech2 behavior), `e4b47a9` (tech2 expanded public build), and `638ff6b` (clean UTF-8 rewrite of published HTML artifacts).
